// Abstraction for the feed.
	/*Scrolling feed object made up of different items.
		
		The Feed is a conveyer belt type object that contains the animations to spit out and manipulate bites.
		var theFeed = {
			init: function(){}
			spit: function(){}
			
		
		Each item is an object (bite).
		var feedBite = {
			init: function(story){
				id = story.id
				title = story.title
				image = story.img
				url = story.url
			}
			updateStory: function(){}
			
			Attributes of a bite:
				var onFeed = Boolean
				var age = float
				var newsSource = string

*/